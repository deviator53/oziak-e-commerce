DATABASE_URL=postgresql://username:password@localhost:5432/oziak_db?sslmode=verify-full
PAYLOAD_SECRET=your-secret-key-here

# Optional: Email configuration for order notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Optional: WhatsApp Business API (if using API instead of direct links)
WHATSAPP_API_URL=
WHATSAPP_API_TOKEN=
